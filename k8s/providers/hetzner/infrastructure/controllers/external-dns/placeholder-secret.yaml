---
# Placeholder secret so external-dns can start during bootstrap.
# ESO overwrites this with real values once the vault chain is operational.
apiVersion: v1
kind: Secret
metadata:
  name: external-dns-cloudflare
  namespace: external-dns
type: Opaque
stringData:
  api-token: "placeholder"
