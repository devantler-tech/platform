---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../bases/bootstrap/
components:
  - ../../../bases/components/annotations-transformers
  # Namespace prune-protection, unchanged for this layer (#3367). These are not
  # tenants and are never removed as a routine act, so blanket protection here
  # costs nothing and still guards against a speculative merge-group deploy.
  - ../../../bases/components/namespace-prune-protection
