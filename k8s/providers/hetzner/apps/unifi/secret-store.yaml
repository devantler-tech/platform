# Tenant-scoped, namespaced SecretStore for the unifi app. The tenant must NOT
# use the shared cluster-scoped `openbao` ClusterSecretStore (whose role can read
# every infra + app path); the `restrict-tenant-secret-stores` Kyverno policy
# blocks tenant ExternalSecrets from referencing it.
#
# Authenticates as the app's own `unifi` ServiceAccount via the dedicated `unifi`
# OpenBao Kubernetes auth role (k8s/bases/infrastructure/vault-config/job.yaml),
# which carries only the `infra-unifi-readonly` policy — read on
# secret/data/infrastructure/unifi/* and nothing else. The tenant can therefore
# reach its controller credentials + WireGuard keys and no other secret.
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: openbao
  namespace: unifi
  labels:
    app.kubernetes.io/managed-by: ksail
spec:
  provider:
    vault:
      # openbao-active = unsealed Raft leader only; the plain `openbao` Service
      # also round-robins onto sealed-but-Ready standbys (503 "Vault is sealed").
      server: "http://openbao-active.openbao.svc.cluster.local:8200"
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "unifi"
          serviceAccountRef:
            name: "unifi"
