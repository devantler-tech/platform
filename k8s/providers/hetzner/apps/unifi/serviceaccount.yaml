---
# tofu-controller spawns its runner pod in this namespace under this SA
# (Terraform.spec.serviceAccountName). The default runner SA name is `tf-runner`.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tf-runner
  namespace: unifi
