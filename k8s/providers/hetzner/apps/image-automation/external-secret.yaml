# Builds the image-automation-git-auth secret for Flux GitHub App auth.
# App ID and installation ID are templated literals (not sensitive); the App
# private key is read from OpenBao at secret/infrastructure/git/image-automation
# (property: private-key). OpenBao is seeded from the SOPS-encrypted key in
# providers/hetzner/infrastructure/vault-seed via a PushSecret.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  labels:
    app.kubernetes.io/managed-by: ksail
  name: image-automation-git-auth
  namespace: flux-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: image-automation-git-auth
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        githubAppID: "1322070"
        githubAppInstallationID: "68329260"
        githubAppPrivateKey: "{{ .privateKey }}"
  data:
    - secretKey: privateKey
      remoteRef:
        key: infrastructure/git/image-automation
        property: private-key
