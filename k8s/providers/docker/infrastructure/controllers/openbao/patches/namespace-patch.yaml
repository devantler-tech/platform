# local-path-provisioner creates short-lived helper pods with hostPath
# volumes when provisioning PVCs — requires privileged PSS.
apiVersion: v1
kind: Namespace
metadata:
  name: openbao
  labels:
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/enforce-version: latest
