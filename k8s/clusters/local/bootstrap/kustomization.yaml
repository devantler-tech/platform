---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../providers/docker/variables
  # Cluster-scoped PriorityClasses (provider-agnostic) — applied by the bootstrap
  # layer so they exist before infrastructure-controllers references them.
  - ../../../bases/infrastructure/priority-classes
  - variables-cluster-config-map.yaml
  - variables-cluster-secret.enc.yaml
