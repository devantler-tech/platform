# Identity for the CronJob failure detector. Its only API grants are the
# per-namespace Roles in role-cronjob-failure-alert-*.yaml.
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cronjob-failure-alert
  namespace: observability
