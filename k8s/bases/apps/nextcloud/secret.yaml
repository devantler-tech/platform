apiVersion: v1
kind: Secret
metadata:
  name: nextcloud
  namespace: nextcloud
type: Opaque
stringData:
  nextcloud-username: ${nextcloud_username:=devantler}
  nextcloud-password: ${nextcloud_password:=SuperSecureAdminPassword2024!}
