apiVersion: v1
kind: Secret
metadata:
  name: nextcloud
  namespace: nextcloud
type: Opaque
stringData:
  nextcloud-username: ${nextcloud_username:=admin}
  nextcloud-password: ${nextcloud_password:=password}
