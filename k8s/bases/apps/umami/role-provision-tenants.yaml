apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: umami-provision-tenants
  namespace: umami
rules:
  - apiGroups:
      - coordination.k8s.io
    resources:
      - leases
    resourceNames:
      - umami-provision-tenants
    verbs:
      - get
      - update
