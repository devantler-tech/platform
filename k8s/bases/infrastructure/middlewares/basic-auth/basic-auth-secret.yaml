apiVersion: v1
kind: Secret
type: kubernetes.io/basic-auth
metadata:
  name: basic-auth
  namespace: traefik
stringData:
  username: ${traefik_basic_auth_user}
  password: ${traefik_basic_auth_password}
