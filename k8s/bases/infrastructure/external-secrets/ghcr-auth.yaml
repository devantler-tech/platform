---
# GHCR pull credentials for Kyverno's image-verification policies
# (verify-app-images.yaml ImageValidatingPolicy): signature manifests
# of PRIVATE first-party images need authenticated GHCR reads. Same OpenBao
# path and shape as the per-app ghcr-auth pull secrets.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ghcr-auth
  namespace: kyverno
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: ghcr-auth
    creationPolicy: Owner
    template:
      engineVersion: v2
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: "{{ .dockerconfigjson }}"
  data:
    - secretKey: dockerconfigjson  # checkov:skip=CKV_SECRET_6:names the key in the synced Secret; the value lives in OpenBao
      remoteRef:
        key: infrastructure/ghcr/auth
        property: dockerconfigjson
