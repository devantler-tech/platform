# Placeholder so Dex can start before ESO creates the real secrets.
# The ExternalSecret in the infrastructure Kustomization overwrites this.
apiVersion: v1
kind: Secret
metadata:
  name: dex-oidc-secrets
  namespace: dex
type: Opaque
stringData:
  github-client-secret: PLACEHOLDER
  dex-client-secret: PLACEHOLDER
  flux-web-client-secret: PLACEHOLDER
