---
# Placeholder secret so Dex can start during bootstrap.
# ESO overwrites this with real values once the vault chain is operational.
apiVersion: v1
kind: Secret
metadata:
  name: dex-oidc-secrets
  namespace: dex
type: Opaque
stringData:
  github-client-secret: "placeholder"
  dex-client-secret: "placeholder"
  flux-web-client-secret: "placeholder"
