# Placeholder so OAuth2-proxy can start before ESO creates the real secrets.
# The ExternalSecret in the infrastructure Kustomization overwrites this.
apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-secrets
  namespace: oauth2-proxy
type: Opaque
stringData:
  client-secret: PLACEHOLDER
  cookie-secret: PLACEHOLDER
