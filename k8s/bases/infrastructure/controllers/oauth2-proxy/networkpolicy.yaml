apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-oauth2-proxy
  namespace: oauth2-proxy
spec:
  endpointSelector:
    matchLabels:
      app: oauth2-proxy
  ingress:
    # Gateway ingress (HTTPRoute -> oauth2-proxy)
    - fromEntities:
        - ingress
      toPorts:
        - ports:
            - port: "4180"
              protocol: TCP
  egress:
    # Auth-proxy upstream (forward authenticated traffic to host-routing proxy)
    - toEndpoints:
        - matchLabels:
            app: auth-proxy
            k8s:io.kubernetes.pod.namespace: oauth2-proxy
      toPorts:
        - ports:
            - port: "8080"
              protocol: TCP
    # Dex token exchange (redeem_url) + JWKS (oidc_jwks_url) over the
    # in-cluster Dex Service. oauth2-proxy does its server-side OIDC calls
    # in-cluster (skip_oidc_discovery; see helm-release.yaml), which the pod
    # can always resolve -- unlike the public dex.${domain} URL, which does
    # not resolve in-cluster on the docker/local cluster.
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: dex
      toPorts:
        - ports:
            - port: "5556"
              protocol: TCP
    # Outbound HTTPS. oauth2-proxy no longer calls Dex or GitHub over the
    # internet (Dex is reached in-cluster above; GitHub is Dex's upstream
    # connector). Keep the public Dex URL reachable as a resilience fallback,
    # but pinned by FQDN instead of world:443 so a compromised pod cannot
    # exfiltrate to arbitrary external services.
    - toFQDNs:
        - matchName: "dex.${domain}"
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
    # Kube API
    - toEntities:
        - kube-apiserver
    # DNS resolution. rules.dns engages Cilium's L7 DNS proxy so the toFQDNs
    # allow-list above can be enforced; matchPattern "*" leaves resolution
    # itself unrestricted (the FQDN list constrains where traffic may go).
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP
          rules:
            dns:
              - matchPattern: "*"
