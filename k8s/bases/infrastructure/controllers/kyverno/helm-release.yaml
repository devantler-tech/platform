apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kyverno
  namespace: kyverno
  labels:
    helm.toolkit.fluxcd.io/crds: enabled
    helm.toolkit.fluxcd.io/helm-test: enabled
    helm.toolkit.fluxcd.io/remediation: enabled
spec:
  interval: 2m
  timeout: 15m
  chart:
    spec:
      chart: kyverno
      version: 3.8.1
      sourceRef:
        kind: HelmRepository
        name: kyverno
  # https://github.com/kyverno/kyverno/blob/main/charts/kyverno/values.yaml
  values:
    # Restricted PSS baseline: kyverno's mutation policies exclude the kyverno namespace,
    # so we must set pod-level seccompProfile + runAsNonRoot via chart values.
    admissionController:
      replicas: ${kyverno_admission_replicas:=2}
      podSecurityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      rbac:
        clusterRole:
          extraResources:
            # The auto-vpa ClusterPolicy generates VPA objects for every
            # Deployment / StatefulSet.  Kyverno needs RBAC on the target
            # resource type for generate rules to work.
            - apiGroups:
                - autoscaling.k8s.io
              resources:
                - verticalpodautoscalers
              verbs:
                - get
                - list
                - watch
                - create
                - update
                - patch
                - delete
            # The add-default-deny ClusterPolicy generates CiliumNetworkPolicy
            # objects in every namespace.
            - apiGroups:
                - cilium.io
              resources:
                - ciliumnetworkpolicies
              verbs:
                - get
                - list
                - watch
                - create
                - update
                - patch
                - delete
      updateStrategy:
        type: RollingUpdate
        rollingUpdate:
          maxSurge: 1
          maxUnavailable: 0
      podDisruptionBudget:
        enabled: true
        # Stays minAvailable: 1: ksail bootstrap-installs kyverno (ksail.yaml
        # policyEngine: Kyverno) with its default minAvailable PDB before Flux,
        # and the chart itself refuses to render both fields. Drain-safe at the
        # 2-replica admission floor. Exempt from validate-pdb-drain-safe.
        minAvailable: 1
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: admission-controller
              app.kubernetes.io/instance: kyverno
    backgroundController:
      replicas: ${kyverno_background_replicas:=1}
      # The chart auto-enables this controller's PDB whenever replicas > 1 and
      # defaults it to minAvailable: 1 -- the exact shape validate-pdb-drain-safe
      # flags. Pin the drain-safe house pattern (#1880/#1882) instead; the chart
      # refuses to render both fields, so only maxUnavailable is set. At the
      # local/CI default of 1 replica the chart renders no PDB at all.
      podDisruptionBudget:
        maxUnavailable: 1
      podSecurityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      rbac:
        clusterRole:
          extraResources:
            - apiGroups:
                - autoscaling.k8s.io
              resources:
                - verticalpodautoscalers
              verbs:
                - get
                - list
                - watch
                - create
                - update
                - patch
                - delete
            - apiGroups:
                - cilium.io
              resources:
                - ciliumnetworkpolicies
              verbs:
                - get
                - list
                - watch
                - create
                - update
                - patch
                - delete
            # disable-default-sa-automount mutates existing `default` SAs
            # in every namespace.
            - apiGroups:
                - ""
              resources:
                - serviceaccounts
              verbs:
                - get
                - list
                - watch
                - update
                - patch
    reportsController:
      replicas: ${kyverno_reports_replicas:=1}
      # Same as backgroundController: override the chart's auto-enabled
      # minAvailable: 1 PDB with the drain-safe maxUnavailable pattern.
      podDisruptionBudget:
        maxUnavailable: 1
      podSecurityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
    cleanupController:
      replicas: ${kyverno_cleanup_replicas:=1}
      # Same as backgroundController: override the chart's auto-enabled
      # minAvailable: 1 PDB with the drain-safe maxUnavailable pattern.
      podDisruptionBudget:
        maxUnavailable: 1
      podSecurityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
    crds:
      migration:
        # The kyverno-migrate-resources post-upgrade hook job sets seccompProfile
        # only at the container level; our require-seccomp-profile ClusterPolicy
        # checks pod-level securityContext, so set it here too (chart default is {})
        # to keep the hook pod compliant and stop background-scan PolicyViolations.
        podSecurityContext:
          runAsNonRoot: true
          seccompProfile:
            type: RuntimeDefault
