# Placeholder so Flux Operator can start before ESO creates the real secret.
# The ExternalSecret in the infrastructure Kustomization overwrites this.
apiVersion: v1
kind: Secret
metadata:
  name: flux-operator-oidc
  namespace: flux-system
type: Opaque
stringData:
  client-secret: PLACEHOLDER
