---
# Placeholder secret so flux-operator can start during bootstrap.
# ESO overwrites this with real values once the vault chain is operational.
apiVersion: v1
kind: Secret
metadata:
  name: flux-operator-oidc
  namespace: flux-system
type: Opaque
stringData:
  client-secret: "placeholder"
