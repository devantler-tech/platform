# Talos worker labels. KubeNodeConfig is the Talos 1.14 representation used to
# render kubelet registration, so Longhorn can match workers on first boot.
apiVersion: v1alpha1
kind: KubeNodeConfig
labels:
  node.longhorn.io/create-default-disk: "true"
