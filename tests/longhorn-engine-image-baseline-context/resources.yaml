# The live shape measured against prod 2026-09-21: an EMPTY pod securityContext
# and one privileged container. Every existing field must survive the retrofit.
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: engine-image-ei-493e04e7
  namespace: longhorn-system
spec:
  selector:
    matchLabels:
      longhorn.io/engine-image: ei-493e04e7
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 100%
  template:
    metadata:
      labels:
        longhorn.io/engine-image: ei-493e04e7
    spec:
      securityContext: {}
      containers:
        - name: engine-image-ei-493e04e7
          image: longhorn-engine:v1.12.1
          securityContext:
            privileged: true
