---
apiVersion: ksail.io/v1alpha1
kind: Cluster
metadata:
  name: devantler-tech-platform-local
spec:
  connection:
    context: kind-devantler-tech-platform-local
  project:
    kustomizationPath: k8s/clusters/local
    deploymentTool: Flux
    cni: Cilium
    ingressController: Traefik
    secretManager: SOPS
