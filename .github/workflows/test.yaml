name: Test

on:
  pull_request:
    branches:
      - "main"
  push:
    branches:
      - "main"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-e2e:
    uses: devantler-tech/.github/.github/workflows/gitops-test.yaml@main
    secrets: inherit
