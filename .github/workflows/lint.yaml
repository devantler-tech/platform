name: Lint
on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  merge_group:

jobs:
  call-gitops-lint-workflow:
    uses: devantler-tech/.github/.github/workflows/gitops-lint.yaml@main
