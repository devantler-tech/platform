name: Sync Cluster Policies

on:
  schedule:
    - cron: "0 0 * * *" # Runs daily at midnight
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "k8s/bases/infrastructure/cluster-policies/samples/**"

permissions: {}

jobs:
  sync-policies:
    uses: devantler-tech/actions/.github/workflows/sync-cluster-policies.yaml@0de1d284f879d62d6fe316d3997fd3ea8f092fb3 # v13.2.1
    with:
      kyverno-policies-dir: k8s/bases/infrastructure/cluster-policies/samples
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
