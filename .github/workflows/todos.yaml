name: "TODOs"
on:
  workflow_dispatch:
  push:
    branches: [main]

permissions: {}

jobs:
  todos:
    uses: devantler-tech/reusable-workflows/.github/workflows/scan-for-todo-comments.yaml@11f70d3be475887db4f262f415fcb19c72a1fa73 # v3.0.1
    permissions:
      issues: write # create/update TODO issues
    secrets:
      app-private-key: ${{ secrets.APP_PRIVATE_KEY }}
