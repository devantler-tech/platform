name: "TODOs"
permissions:
  issues: write
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  todos:
    uses: devantler-tech/github-actions/.github/workflows/todos.yaml@main
    secrets: inherit
