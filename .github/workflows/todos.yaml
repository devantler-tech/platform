name: "TODOs"
on:
  workflow_dispatch:
  push:
    branches: [main]

permissions: {}

jobs:
  todos:
    uses: devantler-tech/reusable-workflows/.github/workflows/scan-for-todo-comments.yaml@5a334687e73feec66012e62db518716a8618417a # v3.0.0
    permissions:
      issues: write # create/update TODO issues
    secrets:
      app-private-key: ${{ secrets.APP_PRIVATE_KEY }}
