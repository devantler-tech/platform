name: "TODOs"
on:
  workflow_dispatch:
  push:
    branches: [main]

permissions: {}

jobs:
  todos:
    uses: devantler-tech/reusable-workflows/.github/workflows/scan-for-todo-comments.yaml@3b781dc75a8218d568ddbd4b92aee971bf9e835d # v3.1.3
    permissions:
      issues: write # create/update TODO issues
    secrets:
      app-private-key: ${{ secrets.APP_PRIVATE_KEY }}
